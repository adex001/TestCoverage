const chai = require('chai');
const assert = chai.assert;
const app = require('../app');

describe('Gets the homepage', function(req, res){
    it('should return the message', function(){
        
    })
});